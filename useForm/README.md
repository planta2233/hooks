#useForm

example:
```
    const initialForm = {
        name: 'loremp dor'
        age: 24,
        email: 'example@example.com'
    }
    const [ values, handleInputChange, reset ] = useForm( initialForm )

```